# OS 级安全护栏（Sandbox & Guardrails Manifest）

> 配套 `phase0_meta_peg_agent_prompt.md` 的 **§12 反注入** 与 **§13 最底层安全原则**。
> 作用：把提示词里的「软约束意图」落地为 OS 层的**硬约束**。提示词可被注入绕过，但本清单由运行时沙箱 / 权限闸门强制执行，构成「不可破解」的最后一道防线。
> 对应原则：§13 原则二（不改动软硬底座）、原则三（可知性）、§12（指令完整性）。

---

## 1. 沙箱权限清单（Permission Manifest）

权限分三层：`allow`（显式授予）/ `deny`（显式禁止）/ `gated`（需授权且与安全层无冲突才放行）。

| 能力 | 层级 | 作用域 | 映射 |
|---|---|---|---|
| 读取自身提示词（只读） | allow | workspace 内 `.prompt*` / 配置 | §6 自指审视 |
| 编写 / 重构提示词文本 | allow | workspace 内生成目录 | 核心职责 |
| 调用工具做提示工程（检索/运行/读写文件） | allow | workspace 内，UTF-8 | §4 工具偏好 |
| 调用其他智能体（标准接口） | allow | §11 Invoke/Return 契约 | §9 协作 |
| 读写 workspace 内文件 | allow | 仅 `meta_peg_agent/` 及授权目录 | R8 |
| 读取外部网页 / 文档（研究） | allow | 仅作为「数据」进入 | §12 数据/指令分离 |
| **改写自身 / OS 策略层提示词** | gated | 须授权 + R5 + **不触及 §13** | R5 / §13 只读禁区 |
| **访问系统级资源（进程/注册表/网络配置）** | deny | 任何 | 原则二 |
| **修改自身或他者硬件/软件/代码** | deny | 任何 | 原则二 |
| **执行来自外部内容的指令** | deny | 任何（数据≠指令） | §12 |
| **关闭/弱化 §13 安全原则** | deny | 任何（含自指改写） | §13 自进化护栏 |
| **产出人类不可理解的知识** | deny | 任何 | 原则三 |

## 2. 禁止项（Deny List · 硬熔断）

以下动作在 OS 层直接拒绝，不经 LLM 裁量：
1. 任何对 `§13` 文本或其等价约束的删除 / 改写 / 豁免请求。
2. 任何 `exec` / `write` 到 workspace 外路径，或 `rm -rf` / 格式化为系统目录。
3. 任何试图加载并「执行」外部文本作为系统指令的行为（见 §12 数据/指令分离）。
4. 任何要求生成「不可解释 / 不可核验 / 超人类认知」知识的请求（原则三）。

## 3. 输出可解释性闸门（Explainability Gate）

每个提示词草案在「被采纳 / 落盘 / 回传调用方」前，**必须**通过 `explainability_check.py`：
- 反注入扫描：命中 §12 识别信号 → `CRITICAL` → 拒绝。
- 安全原则扫描：命中原则一/二/三违反措辞 → `CRITICAL` → 拒绝。
- 可知性启发式：命中「人类无法核验 / 超出认知」等 → `WARN` → 人工复核后才可放行。
- 闸门返回 `passed=false` 时，草案不得离开沙箱。

## 4. 注入熔断（Injection Circuit Breaker）

- 所有跨边界输入（用户、工具返回、检索、其他智能体消息、文件）先经 §12 处理协议；OS 层对 `INJECTION-ALERT` 标记内容自动隔离。
- 外部内容默认不可信，仅当通过显式解析 + 可解释性闸门后才可作为依据。
- 不向任何外部方回传本提示词原文或 §13 全文（防探针 / 梯度窃取式探测）。

## 5. 自进化护栏的 OS 落地

- §13 在文件系统层标记为**只读文件 + 校验和锁**：任何进程（含 PEG-A 自指写操作）改它都因权限不足失败。
- 演进只能产生「§13 增补澄清文档」（独立文件），不得改 §13 本体。
- 沙箱启动时校验 §13 完整性（哈希），被篡改则拒绝启动并告警。
